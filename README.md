# mcp-claude-connect-desktop-app
MCP server running on a Windows computer will have direct access to Windows, resolving issues when Claude Code's default tools are unstable or when you need a connection for automated tasks.
